import {heroui} from '@heroui/react';

/** @type {import('@heroui/react').HeroUIConfig} */
const config = {
  themes: {
    light: {
      colors: {},
    },
    dark: {
      colors: {},
    },
  },
}

export default config;
